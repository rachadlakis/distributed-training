# Core dependencies
torch>=2.5.0
transformers>=4.36.0
accelerate>=0.25.0

# Optional dependencies for data processing
datasets>=2.14.0
tokenizers>=0.15.0

# Optional dependencies for monitoring
tensorboard>=2.15.0
wandb>=0.16.0

# Hugging Face Hub for model downloading
huggingface-hub>=0.20.0

# Additional utilities
numpy>=1.24.0
tqdm>=4.66.0

# For production deployments
sentencepiece>=0.1.99  # For tokenization (Llama, Mistral, etc.)
protobuf>=4.25.0

# Installation instructions:
# Basic installation:
#   pip install -r requirements.txt
#
# For latest PyTorch nightly (recommended for FSDP2):
#   pip install --pre torch --index-url https://download.pytorch.org/whl/nightly/cu121
#   pip install -r requirements.txt
#
# For Hugging Face model access (Llama 2, Mistral, etc.):
#   huggingface-cli login
